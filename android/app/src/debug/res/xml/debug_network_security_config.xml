<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug APKs only: permits the local emulator/LAN HTTP API. Release builds remain HTTPS-only. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
